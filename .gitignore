# Local archive of previous builds — never deploy
_archive/

# macOS junk
.DS_Store
._*

# Editors
.vscode/
.idea/
*.swp

# Node (if ever added)
node_modules/

# Environment secrets (defense in depth)
.env
.env.*
